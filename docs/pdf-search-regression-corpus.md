# PDF 搜索回归语料

最近执行：2026-06-09

设备：HBN-AL80，Android 12

| 场景 | 查询/检查 | 结果 |
|---|---|---|
| 中文文本层 | `搜索关键词` | 命中数量正确，原生矩形与渲染字形相交 |
| 英文文本层 | fixture 中的英文关键词 | 文本与命中矩形均通过 |
| 多行命中 | 跨多个矩形的同一结果 | 所有矩形作为当前结果整组高亮 |
| 旋转页面 | 90 度页面 | 页面为 800×600 显示尺寸，坐标映射通过 |
| 页面坐标 | PDFium 原生 rect → device | 高亮覆盖实际深色字形像素 |
| 跨页导航 | 上一项/下一项 | 当前结果变化时自动跳到对应页 |
| 扫描件 | 无文本层页面 | 标记为 `NEEDS_OCR`，不误报为已索引 |
| 提取器升级 | 旧 `extractorVersion` | 自动失效并调度重建 |

自动化入口：

```powershell
.\gradlew.bat connectedDebugAndroidTest
```

当前限制：尚未集成 OCR；扫描 PDF 只能明确提示“需要 OCR”。长按标注的逐字符位置仍使用 PDFBox 兼容路径，阅读、搜索和 AI 索引均已统一到 PDFium。
